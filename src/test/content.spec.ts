import { findAbsolutePosition } from '../content';

describe('findAbsolutePosition', () => {
  it('returns the absolute position of text in the DOM', () => {
    expect(findAbsolutePosition).toBeDefined();
  });
});
